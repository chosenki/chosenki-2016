const filter = (state = [],action) => {
    switch (action.type){
        case 'FILTER_FIRST':
            return {

            }
        default:
            return state
    }
}

export default filter
